import styled from "styled-components";

const ReplyModal = () => {
  return <ReplyModalContainer>답글 쓰기</ReplyModalContainer>;
};

const ReplyModalContainer = styled.div`
  border: 1px solid red;
  width: 100%;
  height: 100%;
`;

export default ReplyModal;
